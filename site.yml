---
- hosts: all
  become: true
  vars:
      is_container: false

  roles:

      - role: "{{ playbook_dir }}"
        ubtu22cis_system_is_container: "{{ is_container | default(false) }}"
        ubtu22cis_skip_for_travis: false
        ubtu22cis_oscap_scan: yes
