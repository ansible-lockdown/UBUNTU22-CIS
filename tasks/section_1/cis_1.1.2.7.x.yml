---

- name: "1.1.2.7.1 | AUDIT | Ensure separate partition exists for /var/log/audit"
  when:
    - ubtu22cis_rule_1_1_2_7_1
    - required_mount not in mount_names
  tags:
    - level2-server
    - level2-workstation
    - audit
    - mounts
    - rule_1.1.2.7.1
    - NIST800-53R5_CM-7
  vars:
    warn_control_id: '1.1.2.7.1'
    required_mount: '/var/log/audit'
  block:
    - name: "1.1.2.7.1 | AUDIT | Ensure separate partition exists for /var/log/audit | Warn if partition is absent"
      ansible.builtin.debug:
        msg: "Warning!! {{ required_mount }} doesn't exist. Please investigate this manual task"

    - name: "1.1.2.7.1 | AUDIT | Ensure separate partition exists for /var/log/audit | Present"
      ansible.builtin.import_tasks:
        file: warning_facts.yml

# skips if mount is absent
- name: |
    "1.1.2.7.2 | PATCH | Ensure nodev option set on /var/log/audit partition"
    "1.1.2.7.3 | PATCH | Ensure nosuid option set on /var/log/audit partition"
    "1.1.2.7.4 | PATCH | Ensure noexec option set on /var/log/audit partition"
  when:
    - "'/var/log/audit' in mount_names"
    - item.mount == "/var/log/audit"
    - ubtu22cis_rule_1_1_2_7_2 or
      ubtu22cis_rule_1_1_2_7_3 or
      ubtu22cis_rule_1_1_2_7_4
  tags:
    - level1-server
    - level1-workstation
    - patch
    - mounts
    - rule_1.1.2.7.2
    - rule_1.1.2.7.3
    - rule_1.1.2.7.4
    - NIST800-53R5_CM-7
    - NIST800-53R5_AC-3
    - NIST800-53R5_MP-2
  notify: Set_reboot_required
  ansible.posix.mount:
    name: /var/log/audit
    src: "{{ item.device }}"
    fstype: "{{ item.fstype }}"
    state: present
    opts: "{{ item.options }}{% if ('nodev' not in item.options and ubtu22cis_rule_1_1_2_7_2) %},nodev{% endif %}{% if ('nosuid' not in item.options and ubtu22cis_rule_1_1_2_7_3) %},nosuid{% endif %}{% if ('noexec' not in item.options and ubtu22cis_rule_1_1_2_7_4) %},noexec{% endif %}"
  loop: "{{ ansible_facts.mounts }}"
  loop_control:
    label: "{{ item.device }}"
